import { ShopItemProps } from '@/types/data'
import React from 'react'

const BigShopItemBlock = ({ shopItem }: { shopItem: ShopItemProps }) => {
  return <div>BigShopBlock</div>
}

export default BigShopItemBlock
